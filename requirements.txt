torch>=2.1.0
torchvision>=0.16.0
timm>=1.0.0
numpy>=1.24.0,<2
scipy>=1.11.0
scikit-learn>=1.3.0
pyyaml>=6.0
tqdm>=4.66.0
matplotlib>=3.8.0
pandas>=2.1.0

# Dev / test
pytest>=8.0.0
ruff>=0.6.0

# Optional (enable when the corresponding feature is used):
#   peft>=0.11.0    # alternative LoRA backend; a minimal LoRA is built in
#   wandb>=0.17.0   # experiment logging in train_track1 (Milestone 2)
